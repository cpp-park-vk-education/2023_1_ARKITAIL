#include <chrono>
#include <string>

#include "EventManager.hpp"

std::chrono::time_point<std::chrono::steady_clock> EventManager::parseTime(std::string raw_tp) {
    return std::chrono::time_point<std::chrono::steady_clock>();
}

