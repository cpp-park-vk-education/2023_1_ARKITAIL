#include "Calendar.hpp"
#include "CalendarManager.hpp"
#include "Event.hpp"

void CalendarManager::addEvent(const Calendar& calendar, const Event& event) {}

std::vector<Event> CalendarManager::getEvents(const Calendar& calendar) {
    return std::vector<Event>();
}

